// Generated by hkj-processor. Do not edit.
package com.example.bridge;

import java.lang.Override;
import java.lang.String;
import java.lang.UnsupportedOperationException;
import java.util.List;
import java.util.Objects;
import java.util.Optional;
import java.util.function.Function;
import java.util.function.Supplier;
import org.higherkindedj.hkt.nonemptylist.NonEmptyList;
import org.higherkindedj.hkt.validated.FieldError;
import org.higherkindedj.hkt.validated.Validated;
import org.higherkindedj.optics.annotations.Generated;
import org.higherkindedj.optics.validated.ValidatedPrism;

/**
 * Generated bidirectional mapping for {@link CustomerMapping}: total {@code build} and accumulating, located {@code parse}.
 */
@Generated
public final class CustomerMappingImpl implements CustomerMapping {
  public static final CustomerMappingImpl INSTANCE = new CustomerMappingImpl();

  private CustomerMappingImpl() {
  }

  public CustomerDto build(Customer domain) {
    Objects.requireNonNull(domain, "domain must not be null");
    return new CustomerDto(domain.name(), domain.alias().orElse(null), domain.nickname().map(nickname()::build).orElse(null), domain.referrer().map(ReferrerMappingImpl.INSTANCE.asValidatedPrism()::build).orElse(null), domain.referees().map(ReferrerMappingImpl.INSTANCE.asValidatedPrism()::buildAll).orElse(null));
  }

  public Validated<NonEmptyList<FieldError>, Customer> parse(CustomerDto wire) {
    Objects.requireNonNull(wire, "wire must not be null");
    return hkj$construct(
        Validated.fields()
            .field("name", hkj$ifPresent(wire.name(), Validated::validNel))
            .field("alias", Validated.validNel(Optional.ofNullable(wire.alias())))
            .field("nickname", Optional.ofNullable(wire.nickname()).map(v -> nickname().parse(v).map(Optional::of)).orElseGet(() -> Validated.validNel(Optional.empty())))
            .field("referrer", Optional.ofNullable(wire.referrer()).map(v -> ReferrerMappingImpl.INSTANCE.asValidatedPrism().parse(v).map(Optional::of)).orElseGet(() -> Validated.validNel(Optional.empty())))
            .field("referees", Optional.ofNullable(wire.referees()).map(v -> ReferrerMappingImpl.INSTANCE.asValidatedPrism().parseAll(v).map(Optional::of)).orElseGet(() -> Validated.validNel(Optional.empty())))
            .apply((name, alias, nickname, referrer, referees) -> () -> new Customer(name, alias, nickname, referrer, referees)),
        "not a valid Customer");
  }

  /**
   * This mapping as a {@link ValidatedPrism} leaf, so other mappings can nest it directly or lift it through containers.
   */
  public ValidatedPrism<CustomerDto, Customer> asValidatedPrism() {
    return ValidatedPrism.of(this::parse, this::build);
  }

  /**
   * Bridge declaration only; not invocable.
   */
  @Override
  public Optional<String> alias() {
    throw new UnsupportedOperationException("@OptionalBridge markers declare bridges and are not invocable");
  }

  /**
   * Bridge declaration only; not invocable.
   */
  @Override
  public Optional<Referrer> referrer() {
    throw new UnsupportedOperationException("@OptionalBridge markers declare bridges and are not invocable");
  }

  /**
   * Bridge declaration only; not invocable.
   */
  @Override
  public Optional<List<Referrer>> referees() {
    throw new UnsupportedOperationException("@OptionalBridge markers declare bridges and are not invocable");
  }

  /**
   * Guards a nullable read: a null becomes a located {@code FieldError}, otherwise the value is parsed.
   */
  private static <S, A> Validated<NonEmptyList<FieldError>, A> hkj$ifPresent(S value,
      Function<? super S, Validated<NonEmptyList<FieldError>, A>> parse) {
    return value == null ? Validated.invalidNel(FieldError.of("must not be null")) : parse.apply(value);
  }

  /**
   * Runs the canonical constructor over the validated components: an exception it throws is the record's
   * invariant refusing them, reported at the record's path with its message, or with {@code
   * fallbackMessage} when it has none.
   */
  private static <T> Validated<NonEmptyList<FieldError>, T> hkj$construct(
      Validated<NonEmptyList<FieldError>, Supplier<T>> assembled, String fallbackMessage) {
    return assembled.flatMap(
        constructor -> {
          try {
            return Validated.validNel(constructor.get());
          } catch (RuntimeException refused) {
            String message = refused.getMessage();
            return Validated.invalidNel(FieldError.of(message == null || message.isBlank() ? fallbackMessage : message));
          }
        });
  }
}
