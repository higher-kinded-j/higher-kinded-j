// Generated by hkj-processor. Do not edit.
package com.example.patch;

import java.util.Objects;
import java.util.function.Function;
import org.higherkindedj.hkt.nonemptylist.NonEmptyList;
import org.higherkindedj.hkt.validated.FieldError;
import org.higherkindedj.hkt.validated.Validated;
import org.higherkindedj.optics.annotations.Generated;

/**
 * Generated projection mapping for {@link SubscriberDetailsMapping}: total {@code build} and a validated {@code patch} write-back. No {@code parse} is emitted — the dropped components cannot be reconstructed (truthful types).
 */
@Generated
public final class SubscriberDetailsMappingImpl implements SubscriberDetailsMapping {
  public static final SubscriberDetailsMappingImpl INSTANCE = new SubscriberDetailsMappingImpl();

  private SubscriberDetailsMappingImpl() {
  }

  public SubscriberDetailsDto build(Subscriber domain) {
    Objects.requireNonNull(domain, "domain must not be null");
    return new SubscriberDetailsDto(email().build(domain.email()), domain.age());
  }

  /**
   * Writes the wire's projected components onto {@code domain}, validating each one; every bad field is reported at once, located under its component name, and unprojected components stay untouched. Dense: every projected component applies, and a {@code null} reference read is a located error, never absence — contrast {@code UpdateSpec}'s sparse {@code updateFrom}. Nulls locate through nesting too: a nested wire value delegates to the nested spec's parse, whose reference legs carry the same guard.
   */
  public Validated<NonEmptyList<FieldError>, Subscriber> patch(Subscriber domain,
      SubscriberDetailsDto wire) {
    Objects.requireNonNull(domain, "domain must not be null");
    Objects.requireNonNull(wire, "wire must not be null");
    return Validated.fields()
        .field("email", hkj$ifPresent(wire.email(), email()::parse))
        .field("age", Validated.validNel(wire.age()))
        .apply((email, age) -> new Subscriber(domain.id(), email, age));
  }

  /**
   * Guards a nullable read: a null becomes a located {@code FieldError}, otherwise the value is parsed.
   */
  private static <S, A> Validated<NonEmptyList<FieldError>, A> hkj$ifPresent(S value,
      Function<? super S, Validated<NonEmptyList<FieldError>, A>> parse) {
    return value == null ? Validated.invalidNel(FieldError.of("must not be null")) : parse.apply(value);
  }
}
