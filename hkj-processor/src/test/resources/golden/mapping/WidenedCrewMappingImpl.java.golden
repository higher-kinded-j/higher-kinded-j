// Generated by hkj-processor. Do not edit.
package com.example.widened;

import java.lang.Object;
import java.lang.String;
import java.util.Collection;
import java.util.Collections;
import java.util.LinkedHashSet;
import java.util.Objects;
import java.util.Set;
import java.util.function.Function;
import java.util.function.Supplier;
import org.higherkindedj.hkt.nonemptylist.NonEmptyList;
import org.higherkindedj.hkt.validated.FieldError;
import org.higherkindedj.hkt.validated.Validated;
import org.higherkindedj.optics.annotations.Generated;
import org.higherkindedj.optics.validated.ValidatedPrism;

/**
 * Generated bidirectional mapping for {@link CrewMapping}: total {@code build} and accumulating, located {@code parse}.
 */
@Generated
public final class CrewMappingImpl implements CrewMapping {
  public static final CrewMappingImpl INSTANCE = new CrewMappingImpl();

  private CrewMappingImpl() {
  }

  public CrewDto build(Crew domain) {
    Objects.requireNonNull(domain, "domain must not be null");
    return new CrewDto(members().buildAll(domain.members()), reserves().buildAll(domain.reserves(), String[]::new), hkj$copyOf(domain.tags()), hkj$copyOf(domain.codes()), hkj$copyOf(domain.ranks()), noteKey().buildKeys(domain.notes()), escalationKey().buildEntries(domain.escalations(), escalations()));
  }

  public Validated<NonEmptyList<FieldError>, Crew> parse(CrewDto wire) {
    Objects.requireNonNull(wire, "wire must not be null");
    return hkj$construct(
        Validated.fields()
            .field("members", hkj$ifPresent(wire.members(), members()::parseAll))
            .field("reserves", hkj$ifPresent(wire.reserves(), v -> reserves().parseAll(v, Email[]::new)))
            .field("tags", hkj$allPresent(hkj$copyOf(wire.tags())))
            .field("codes", hkj$allPresent(hkj$copyOf(wire.codes())))
            .field("ranks", hkj$ifPresent(hkj$copyOf(wire.ranks()), Validated::validNel))
            .field("notes", hkj$ifPresent(wire.notes(), noteKey()::parseKeys))
            .field("escalations", hkj$ifPresent(wire.escalations(), v -> escalationKey().parseEntries(v, escalations())))
            .apply((members, reserves, tags, codes, ranks, notes, escalations) -> () -> new Crew(members, reserves, tags, codes, ranks, notes, escalations)),
        "not a valid Crew");
  }

  /**
   * This mapping as a {@link ValidatedPrism} leaf, so other mappings can nest it directly or lift it through containers.
   */
  public ValidatedPrism<CrewDto, Crew> asValidatedPrism() {
    return ValidatedPrism.of(this::parse, this::build);
  }

  /**
   * Guards a nullable read: a null becomes a located {@code FieldError}, otherwise the value is parsed.
   */
  private static <S, A> Validated<NonEmptyList<FieldError>, A> hkj$ifPresent(S value,
      Function<? super S, Validated<NonEmptyList<FieldError>, A>> parse) {
    return value == null ? Validated.invalidNel(FieldError.of("must not be null")) : parse.apply(value);
  }

  /**
   * Guards an identity-copied collection: a null element is a located invalid at its position, accumulating, or, in a set, which has no positions, the unlocated {@code must not contain a null element}.
   */
  private static <C extends Collection<?>> Validated<NonEmptyList<FieldError>, C> hkj$allPresent(
      C values) {
    if (values == null) {
      return Validated.invalidNel(FieldError.of("must not be null"));
    }
    boolean set = values instanceof Set;
    NonEmptyList<FieldError> failures = null;
    int i = 0;
    for (Object element : values) {
      if (element == null) {
        NonEmptyList<FieldError> located = NonEmptyList.of(set ? FieldError.of("must not contain a null element") : FieldError.of("must not be null").at(String.valueOf(i)));
        failures = failures == null ? located : NonEmptyList.<FieldError>semigroup().combine(failures, located);
      }
      i++;
    }
    return failures == null ? Validated.valid(values) : Validated.invalid(failures);
  }

  /**
   * Guards an identity-copied array: a null element is a located invalid at its index, accumulating.
   */
  private static <E> Validated<NonEmptyList<FieldError>, E[]> hkj$allPresent(E[] values) {
    if (values == null) {
      return Validated.invalidNel(FieldError.of("must not be null"));
    }
    NonEmptyList<FieldError> failures = null;
    for (int i = 0; i < values.length; i++) {
      if (values[i] == null) {
        NonEmptyList<FieldError> located = NonEmptyList.of(FieldError.of("must not be null").at(String.valueOf(i)));
        failures = failures == null ? located : NonEmptyList.<FieldError>semigroup().combine(failures, located);
      }
    }
    return failures == null ? Validated.valid(values) : Validated.invalid(failures);
  }

  /**
   * Runs the canonical constructor over the validated components: an exception it throws is the record's
   * invariant refusing them, reported at the record's path with its message, or with {@code
   * fallbackMessage} when it has none.
   */
  private static <T> Validated<NonEmptyList<FieldError>, T> hkj$construct(
      Validated<NonEmptyList<FieldError>, Supplier<T>> assembled, String fallbackMessage) {
    return assembled.flatMap(
        constructor -> {
          try {
            return Validated.validNel(constructor.get());
          } catch (RuntimeException refused) {
            String message = refused.getMessage();
            return Validated.invalidNel(FieldError.of(message == null || message.isBlank() ? fallbackMessage : message));
          }
        });
  }

  /**
   * Copies a same-typed set so the two sides never share it: an unmodifiable set in the same iteration order, a null element included; a null set copies to null.
   */
  private static <E> Set<E> hkj$copyOf(Set<? extends E> values) {
    return values == null ? null : Collections.unmodifiableSet(new LinkedHashSet<>(values));
  }

  /**
   * Copies a same-typed array so the two sides never share it: a clone, null elements included; a null array copies to null.
   */
  private static <E> E[] hkj$copyOf(E[] values) {
    return values == null ? null : values.clone();
  }

  /**
   * Copies a same-typed array so the two sides never share it: a clone; a null array copies to null.
   */
  private static int[] hkj$copyOf(int[] values) {
    return values == null ? null : values.clone();
  }
}
