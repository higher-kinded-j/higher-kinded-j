// Generated by hkj-processor. Do not edit.
package com.example.oneway;

import java.lang.String;
import java.util.List;
import java.util.Objects;
import java.util.Optional;
import java.util.function.Function;
import org.higherkindedj.hkt.nonemptylist.NonEmptyList;
import org.higherkindedj.hkt.validated.FieldError;
import org.higherkindedj.hkt.validated.Validated;
import org.higherkindedj.optics.annotations.Generated;
import org.higherkindedj.optics.validated.ValidatedParse;

/**
 * Generated parse-only mapping for {@link CustomerViewMapping}: accumulating, located {@code parse} and no {@code build}, since the wire offers nothing to write it through (truthful types).
 */
@Generated
public final class CustomerViewMappingImpl implements CustomerViewMapping {
  public static final CustomerViewMappingImpl INSTANCE = new CustomerViewMappingImpl();

  private CustomerViewMappingImpl() {
  }

  public Validated<NonEmptyList<FieldError>, Customer> parse(CustomerView wire) {
    Objects.requireNonNull(wire, "wire must not be null");
    return Validated.fields()
        .field("name", hkj$ifPresent(wire.getName(), Validated::validNel))
        .field("email", hkj$ifPresent(wire.getEmail(), email()::parse))
        .field("tags", hkj$allPresent(wire.getTags()))
        .field("nickname", Validated.validNel(Optional.ofNullable(wire.getNickname())))
        .apply(Customer::new);
  }

  /**
   * This parse-only mapping as a {@link ValidatedParse}, so a mapping that only parses can nest it directly or lift it through containers.
   */
  public ValidatedParse<CustomerView, Customer> asValidatedParse() {
    return ValidatedParse.of(this::parse);
  }

  /**
   * Guards a nullable read: a null becomes a located {@code FieldError}, otherwise the value is parsed.
   */
  private static <S, A> Validated<NonEmptyList<FieldError>, A> hkj$ifPresent(S value,
      Function<? super S, Validated<NonEmptyList<FieldError>, A>> parse) {
    return value == null ? Validated.invalidNel(FieldError.of("must not be null")) : parse.apply(value);
  }

  /**
   * Guards an identity-copied list: a null element is a located invalid at its index, accumulating.
   */
  private static <E> Validated<NonEmptyList<FieldError>, List<E>> hkj$allPresent(List<E> values) {
    if (values == null) {
      return Validated.invalidNel(FieldError.of("must not be null"));
    }
    NonEmptyList<FieldError> failures = null;
    int i = 0;
    for (Object element : values) {
      if (element == null) {
        NonEmptyList<FieldError> located = NonEmptyList.of(FieldError.of("must not be null").at(String.valueOf(i)));
        failures = failures == null ? located : NonEmptyList.<FieldError>semigroup().combine(failures, located);
      }
      i++;
    }
    return failures == null ? Validated.valid(values) : Validated.invalid(failures);
  }
}
