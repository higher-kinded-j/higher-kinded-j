// Generated by hkj-processor. Do not edit.
package com.example.update;

import java.lang.String;
import java.util.Objects;
import org.higherkindedj.optics.Lens;
import org.higherkindedj.optics.Setter;
import org.higherkindedj.optics.annotations.Generated;
import org.higherkindedj.optics.edit.Edit;
import org.higherkindedj.optics.edit.Edits;

/**
 * Generated sparse PATCH write-back for {@link UserPatchMapping}: folds the present wire fields into an {@code Edits.Accumulated<Domain>}.
 */
@Generated
public final class UserPatchMappingImpl implements UserPatchMapping {
  public static final UserPatchMappingImpl INSTANCE = new UserPatchMappingImpl();

  private UserPatchMappingImpl() {
  }

  /**
   * Folds the present (non-null) properties of {@code wire} into an update: an absent property leaves the domain unchanged, a present one is set (or parsed through its leaf) and located on failure. The domain is constructed once, from the values the update ends on.
   */
  public Edits.Accumulated<User> updateFrom(UserPatchDto wire) {
    Objects.requireNonNull(wire, "wire must not be null");
    return Edits.<User, Components>accumulate(
            Lens.of(
                d -> new Components(d.name(), d.email(), d.age()),
                (d, c) -> new User(c.name(), c.email(), c.age())),
            Edit.setIfPresent(Setter.fromGetSet(Components::name, (c, v) -> new Components(v, c.email(), c.age())), wire.getName()),
            Edit.parseIfPresent(Setter.fromGetSet(Components::email, (c, v) -> new Components(c.name(), v, c.age())), wire.getEmail(), email()::parse).at("email"),
            Edit.setIfPresent(Setter.fromGetSet(Components::age, (c, v) -> new Components(c.name(), c.email(), v)), wire.getAge()));
  }

  /**
   * The components of {@link User} a PATCH can write, which {@code updateFrom}'s setters write before the domain is constructed once.
   */
  @Generated
  private record Components(String name, EmailAddress email, int age) {
  }
}
