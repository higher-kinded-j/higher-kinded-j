// Generated by hkj-processor. Do not edit.
package org.higherkindedj.hkt.expression;

import java.util.Objects;
import java.util.function.Function;
import java.util.function.BiFunction;
import org.higherkindedj.hkt.Kind;
import org.higherkindedj.hkt.Monad;
import org.higherkindedj.hkt.Traverse;
import org.higherkindedj.hkt.TypeArity;
import org.higherkindedj.hkt.WitnessArity;
import org.higherkindedj.hkt.tuple.Tuple;
import org.higherkindedj.hkt.tuple.Tuple2;
import org.higherkindedj.hkt.tuple.Tuple3;
import org.higherkindedj.hkt.tuple.Tuple4;
import org.higherkindedj.hkt.tuple.Tuple5;
import org.higherkindedj.hkt.tuple.Tuple6;
import org.higherkindedj.optics.annotations.Generated;

/** Step 2 in a non-filterable for-comprehension. */
@Generated
public final class MonadicSteps2<M extends WitnessArity<TypeArity.Unary>, A, B>
    implements For.Steps<M> {

  private final Monad<M> monad;
  private final Kind<M, Tuple2<A, B>> computation;

  MonadicSteps2(Monad<M> monad, Kind<M, Tuple2<A, B>> computation) {
    this.monad = monad;
    this.computation = computation;
  }

  public <C> MonadicSteps3<M, A, B, C> from(
      Function<Tuple2<A, B>, Kind<M, C>> next) {
    Kind<M, Tuple3<A, B, C>> newComputation =
        monad.flatMap(
            t -> monad.map(
                c -> Tuple.of(t._1(), t._2(), c),
                next.apply(t)),
            this.computation);
    return new MonadicSteps3<>(monad, newComputation);
  }

  public <C> MonadicSteps3<M, A, B, C> let(
      Function<Tuple2<A, B>, C> f) {
    Kind<M, Tuple3<A, B, C>> newComputation =
        monad.map(
            t -> Tuple.of(t._1(), t._2(), f.apply(t)),
            this.computation);
    return new MonadicSteps3<>(monad, newComputation);
  }

  public <C> MonadicSteps3<M, A, B, C> focus(
      Function<Tuple2<A, B>, C> extractor) {
    Objects.requireNonNull(extractor, "extractor must not be null");
    Kind<M, Tuple3<A, B, C>> newComputation =
        monad.map(
            t -> Tuple.of(t._1(), t._2(), extractor.apply(t)),
            this.computation);
    return new MonadicSteps3<>(monad, newComputation);
  }

  public <C, D> MonadicSteps4<M, A, B, C, D> par(
      Function<Tuple2<A, B>, Kind<M, C>> f1,
      Function<Tuple2<A, B>, Kind<M, D>> f2) {
    Kind<M, Tuple4<A, B, C, D>> next =
        monad.flatMap(
            t -> monad.map2(
                f1.apply(t),
                f2.apply(t),
                (r1, r2) -> Tuple.of(t._1(), t._2(), r1, r2)),
            this.computation);
    return new MonadicSteps4<>(monad, next);
  }

  public <C, D, E> MonadicSteps5<M, A, B, C, D, E> par(
      Function<Tuple2<A, B>, Kind<M, C>> f1,
      Function<Tuple2<A, B>, Kind<M, D>> f2,
      Function<Tuple2<A, B>, Kind<M, E>> f3) {
    Kind<M, Tuple5<A, B, C, D, E>> next =
        monad.flatMap(
            t -> monad.map3(
                f1.apply(t),
                f2.apply(t),
                f3.apply(t),
                (r1, r2, r3) -> Tuple.of(t._1(), t._2(), r1, r2, r3)),
            this.computation);
    return new MonadicSteps5<>(monad, next);
  }

  public <C, D, E, F> MonadicSteps6<M, A, B, C, D, E, F> par(
      Function<Tuple2<A, B>, Kind<M, C>> f1,
      Function<Tuple2<A, B>, Kind<M, D>> f2,
      Function<Tuple2<A, B>, Kind<M, E>> f3,
      Function<Tuple2<A, B>, Kind<M, F>> f4) {
    Kind<M, Tuple6<A, B, C, D, E, F>> next =
        monad.flatMap(
            t -> monad.map4(
                f1.apply(t),
                f2.apply(t),
                f3.apply(t),
                f4.apply(t),
                (r1, r2, r3, r4) -> Tuple.of(t._1(), t._2(), r1, r2, r3, r4)),
            this.computation);
    return new MonadicSteps6<>(monad, next);
  }

  public <TT extends WitnessArity<TypeArity.Unary>, TC, TR> MonadicSteps3<M, A, B, Kind<TT, TR>> traverse(
      Traverse<TT> traversable,
      Function<Tuple2<A, B>, Kind<TT, TC>> extractor,
      Function<TC, Kind<M, TR>> f) {
    Objects.requireNonNull(traversable, "traversable must not be null");
    Objects.requireNonNull(extractor, "extractor must not be null");
    Objects.requireNonNull(f, "function must not be null");
    Kind<M, Tuple3<A, B, Kind<TT, TR>>> newComputation =
        monad.flatMap(
            t ->
                monad.map(
                    tb -> Tuple.of(t._1(), t._2(), tb),
                    traversable.traverse(monad, f, extractor.apply(t))),
            this.computation);
    return new MonadicSteps3<>(monad, newComputation);
  }

  public <TT extends WitnessArity<TypeArity.Unary>, TR> MonadicSteps3<M, A, B, Kind<TT, TR>> sequence(
      Traverse<TT> traversable,
      Function<Tuple2<A, B>, Kind<TT, Kind<M, TR>>> extractor) {
    Objects.requireNonNull(traversable, "traversable must not be null");
    Objects.requireNonNull(extractor, "extractor must not be null");
    Kind<M, Tuple3<A, B, Kind<TT, TR>>> newComputation =
        monad.flatMap(
            t ->
                monad.map(
                    tb -> Tuple.of(t._1(), t._2(), tb),
                    traversable.sequenceA(monad, extractor.apply(t))),
            this.computation);
    return new MonadicSteps3<>(monad, newComputation);
  }

  public <TT extends WitnessArity<TypeArity.Unary>, TC, TR> MonadicSteps3<M, A, B, Kind<TT, TR>> flatTraverse(
      Traverse<TT> traversable,
      Monad<TT> innerMonad,
      Function<Tuple2<A, B>, Kind<TT, TC>> extractor,
      Function<TC, Kind<M, Kind<TT, TR>>> f) {
    Objects.requireNonNull(traversable, "traversable must not be null");
    Objects.requireNonNull(innerMonad, "innerMonad must not be null");
    Objects.requireNonNull(extractor, "extractor must not be null");
    Objects.requireNonNull(f, "function must not be null");
    Kind<M, Tuple3<A, B, Kind<TT, TR>>> newComputation =
        monad.flatMap(
            t -> {
              Kind<M, Kind<TT, Kind<TT, TR>>> traversed =
                  traversable.traverse(monad, f, extractor.apply(t));
              return monad.map(
                  ttb -> Tuple.of(t._1(), t._2(), innerMonad.flatMap(Function.identity(), ttb)),
                  traversed);
            },
            this.computation);
    return new MonadicSteps3<>(monad, newComputation);
  }

  public <S> ForState.Steps<M, S> toState(BiFunction<A, B, S> constructor) {
    Objects.requireNonNull(constructor, "constructor must not be null");
    return ForState.withState(monad, monad.map(
        t -> constructor.apply(t._1(), t._2()),
        computation));
  }

  public <S> ForState.Steps<M, S> toState(Function<Tuple2<A, B>, S> constructor) {
    Objects.requireNonNull(constructor, "constructor must not be null");
    return ForState.withState(monad, monad.map(constructor, computation));
  }

  public <R> Kind<M, R> yield(BiFunction<A, B, R> f) {
    return monad.map(
        t -> Objects.requireNonNull(
            f.apply(t._1(), t._2()),
            "The yield function must not return null."),
        computation);
  }

  public <R> Kind<M, R> yield(Function<Tuple2<A, B>, R> f) {
    return monad.map(f, computation);
  }
}
